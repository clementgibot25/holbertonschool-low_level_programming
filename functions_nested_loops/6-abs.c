#include "main.h"

/**
 * _abs - function that computes the absolute value
 * of an integer
 *
 * @n: number being checked
 *
 * Return: 0;
 */
int _abs(int n)
{
	if (n < 0)
	_putchar(n * -1);
	return (0);
}
